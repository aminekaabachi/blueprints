<section class="newsletter-section">
  <div class="newsletter-content">
    <h2 class="newsletter-heading">{{ site.newsletter.heading }}</h2>
    <p class="newsletter-description">
Subscribe to Blueprints and receive focused essays on Data & AI system architecture. I (try to) deliver the structural insights and essential thinking you need to build better systems.    </p>
    <form class="newsletter-form" action="#" method="POST">
      <div class="newsletter-input-group">
        <input 
          type="email" 
          class="newsletter-input" 
          placeholder="{{ site.newsletter.placeholder }}" 
          required
          aria-label="Email address"
        >
        <button type="submit" class="newsletter-button">{{ site.newsletter.button }}</button>
      </div>
    </form>
  </div>
</section>