<section class="newsletter-section">
  <div class="newsletter-content">
    <h2 class="newsletter-heading">{{ site.newsletter.heading }}</h2>
    <p class="newsletter-description">
Get occasional notes from SkewSkew with reflections on data systems, hard-won lessons, and tools that actually pull their weight. It’s for the quietly obsessed: the ones who care about how things really get built.

    </p>
    <form class="newsletter-form" action="#" method="POST">
      <div class="newsletter-input-group">
        <input 
          type="email" 
          class="newsletter-input" 
          placeholder="{{ site.newsletter.placeholder }}" 
          required
          aria-label="Email address"
        >
        <button type="submit" class="newsletter-button">{{ site.newsletter.button }}</button>
      </div>
    </form>
  </div>
</section>