---
layout: layout.njk
title: Home
---
<h2>Latest Posts</h2>
<ul>
  {% for post in collections.post | reverse %}
    <li><a href="{{ post.url }}">{{ post.data.title }}</a> - <time datetime="{{ post.date | date('YYYY-MM-DD') }}">{{ post.date | date("MMMM D, YYYY") }}</time></li>
  {% endfor %}
</ul>

